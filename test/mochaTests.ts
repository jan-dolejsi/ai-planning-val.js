const expect = require('chai').expect;
// import { MyClass } from '../src/MyClass';

describe("MyClass", () => {
    describe("#method()", () => {
        it("does something", () => {
            // new MyClass();
            //expect.
        });
    });
});